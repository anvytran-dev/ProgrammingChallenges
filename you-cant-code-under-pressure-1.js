//P: There is one paramter: i. i is an integer.
//R: Return double the integer.
//E: i = 3, => 6
//P: Use an arithmetic operator to double the integer. 
function doubleInteger(i) {
  
  return i *= 2;
}
