//P: The paramenter is a variable holding a string.
//R: Return the string in uppercase.
//E: If string is 'jump,' return 'JUMP.'
//P: Use the .toUpperCase(); method to conver the stirng to uppercase.

function makeUpperCase(str) {
  
  return str.toUpperCase();
  
}
