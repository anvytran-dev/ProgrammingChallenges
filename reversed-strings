//P: String
//R: Return the string in reverse
//E: "bunny" --> "yunnb"
//P: First, use the split method to put the string into an array and to split it by character.
//   Next, ise the reverse method to reverse the array.
//   Then, use the join method to join the string back together in order to return a reversed string. 

function solution(string){
  return string.split("").reverse().join("");
}
