//P: There is one parameter. The parameter is an array of integers.
//R: Return a new array with each value doubled from the original array.
//E: [2, 3, 4] --> [4, 6, 8]
//P: Use the .map(); method to create a new array that is populated with the results of calling a function that doubles the value of every element in the calling array. 



function maps(x) {
  
  return x.map(x => x * 2)
  
  
}
